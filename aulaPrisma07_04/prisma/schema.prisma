generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}

model Teste {
  id    Int    @id @default(autoincrement())
  nome  String
  email String @unique

  @@map ("teste")
}
